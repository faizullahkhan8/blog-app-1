import React from "react";
import style from "./style.module.css";

const Footer = () => {
    return (
        <footer className={style.footerWrapper}>
            Made with &hearts; and MERN stack
        </footer>
    );
};

export default Footer;
